{% import "_includes/forms" as forms %}

{{ siteUrl }}<br>
{{ siteUrl }}<br>

{{ forms.textField({
    label: 'Client ID',
    id: 'clientId',
    name: 'clientId', 
    required: true,
    value: settings['clientId']})
}}

{{ forms.textField({
    label: 'Client Secret',
    id: 'clientSecret',
    name: 'clientSecret', 
    required: true,
    value: settings['clientSecret']})
}}
